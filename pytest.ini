[pytest]
testpaths = tests
pythonpath = .
markers =
    real_cli: optional tests that call the live @microsoft/events-cli (skipped by default)
addopts = -q -m "not real_cli"
filterwarnings =
    ignore::DeprecationWarning
    ignore:Using `httpx` with `starlette.testclient` is deprecated:Warning
